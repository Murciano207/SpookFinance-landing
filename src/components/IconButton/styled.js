import styled from 'styled-components';

const IconButton = styled.a`
  color: #fff;
  cursor: pointer;
  border: none;
  outline: none;
  font-size: 2rem;
  width: 2rem;
  height: 2rem;

  & svg {
    height: 100%;
  }
`;

export default IconButton;
